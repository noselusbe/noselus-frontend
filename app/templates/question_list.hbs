<div {{bind-attr class='questionHasAnswer:activityfeed--story-answered :activityfeed--story'}}>
  <div class="media">
    <div class="pull-left">
      <div {{bind-attr class='askedBy.assembly :img-circle :thumbnail :avatar :pull-left'}}></div>
      {{#link-to 'politician' askedBy class='pull-left'}}
        {{{askedBy.thumbImage}}}
      {{/link-to}}
      {{#link-to 'politician' askedTo class='pull-left'}}
        {{{askedTo.thumbImage}}}
      {{/link-to}}
    </div>
    <div class="media-body">
      <header class="activityfeed--header">
        {{#link-to 'question' question tagName='h4'}}
          {{title}}
        {{/link-to}}
      </header>
      <small class="activityfeed--header--authors small">
        Posée
        {{format-date dateAsked}}
        par
        {{#link-to 'politician' askedBy}}
          <strong>{{askedBy.fullName}}</strong>
        {{/link-to}}
        à
        {{#link-to 'politician' askedTo}}
          <strong>{{askedTo.fullName}}</strong>
        {{/link-to}}
      </small>
      {{!-- <small>
        {{#if questionHasAnswer}}
          <strong class="activityfeed--header--authors--answer-added text-success">
            <i class="glyphicon glyphicon-share-alt text-success"></i>
            Réponse ajoutée
          </strong>
        {{else}}
          <span class="activityfeed--header--authors--answer-added">
            <i class="glyphicon glyphicon-share-alt text-muted"></i>
            Pas de réponse
          </span>
        {{/if}}
      </small> --}}
      {{!-- <div class="activityfeed--excerpt">
        <p class='text-muted'>{{{clean-text excerpt}}}</p>
        <p>
          {{#link-to 'question' question class='btn btn-xs btn-default'}}
            Lire la suite
          {{/link-to}}
        </p>
      </div> --}}
    </div>
  </div>
</div>
