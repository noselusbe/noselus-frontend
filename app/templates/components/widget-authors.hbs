<div class="panel panel-default panel-authors">
  <div class="panel-heading">
    Auteurs
  </div>
  <div class="panel-body">
    <div class="media">
      <div class="pull-left">
        {{{askedBy.thumbImage}}}
      </div>
      <div class="media-body">
        {{#link-to 'politician' askedBy}}
          {{askedBy.fullName}}
        {{/link-to}}
        <p>{{askedBy.party}}</p>
      </div>
    </div>
    <div class="media">
      {{#if askedTo.thumbImage}}
        <div class="pull-left">
          {{{askedTo.thumbImage}}}
        </div>
      {{/if}}
      <div class="media-body">
        {{#link-to 'politician' askedTo}}
          {{askedTo.fullName}}
        {{/link-to}}
        <p>{{askedTo.party}}</p>
      </div>
    </div>
  </div>
  <div class="clearfix"></div>
</div>
